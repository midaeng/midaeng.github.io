baseURL: "https://midaeng.github.io/midaeng-hugo/"
title: "BangLab"
languageCode: ko-kr
hasCJKLanguage: true

# [수정포인트 1] 현재 루트를 테마로 사용
themesDir: "."
theme: "."

# [수정포인트 2] RSS가 아닌 HTML을 우선적으로 생성하도록 강제합니다.
# 이 설정이 빠지면 방금처럼 XML 파일이 메인을 차지할 수 있습니다.
outputs:
  home: [HTML, RSS, JSON]
  page: [HTML]
  section: [HTML]
  taxonomy: [HTML]
  term: [HTML]

params:
  defaultTheme: light
  # 에러 방지를 위한 작성자 정보
  author: "Mijin Baek" 

menu:
  main:
    - name: About
      url: /about/
      weight: 10
